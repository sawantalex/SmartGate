# Replace with your MongoDB Atlas credentials
MONGO_URI=mongodb+srv://<USERNAME>:<PASSWORD>@cluster0.povoaon.mongodb.net/smartgateDB?retryWrites=true&w=majority

# JWT secret for admin auth (use a long random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Server port
PORT=5000
